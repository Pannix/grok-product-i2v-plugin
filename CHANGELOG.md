# Changelog

## 0.6.0

- 参考图改为商品、内容物、人物、风格四组，支持每组最多 3 张。
- 分镜图模型会收到全部已分组参考图，并在提示词中明确每组的职责，避免内容物被误当成商品、人物或风格。
- 新增四组参考图数量输入、标题关键词自动识别、未分组参考图提示，同时兼容旧节点的顺序连接方式。

## 0.5.0

- 总时长支持 2–60 秒，分镜数支持自填 1–20。
- 按 Grok/渠道单段上限自动分配镜头时长，并在所有镜头完成后自动合成为一个最终视频。
- 生成与合成阶段统一使用目标画幅；9:16 最终通过 720×1280 浏览器画布录制，避免渠道忽略宽高字段时仍输出横屏。
- New API 分发脚本补充 `size`、`width`、`height`、`aspect_ratio` 和 `metadata` 画幅信息。

## 0.4.1

- 扩展分发网关完成响应的视频地址解析，兼容 `result_url`、`content.video_url`、嵌套 `data` 等字段。
- 找不到视频地址时显示响应字段名，便于区分网关返回结构和任务生成失败。

## 0.4.0

- 新增 New API / 分发网关专用视频脚本：使用 `POST /v1/video/generations`、`image` 字符串和 `GET /v1/video/generations/{task_id}`。
- 新增“复制 New API 分发脚本”按钮，保留原生 xAI 脚本，避免把两种接口格式混用。

## 0.3.0

- 新增“脚本规划 → 分镜首帧图 → Grok 逐镜头视频”的一键工作流。
- 支持按连接顺序区分商品图、人物参考图和风格参考图；人物锁定模式要求第二张人物参考图。
- 支持 2–4 个镜头、独立分镜图模型和视频模型；每张分镜图会作为对应 Grok image-to-video 镜头的实际首帧。
- 新增分镜图/视频节点元数据、批量生成状态、失败镜头提示和逐镜头质检清单。
- 文档补充 xAI image-to-video 与 reference-to-video 的边界，以及 IP-Adapter / FaceID / ControlNet 作为增强路径的适用范围。

## 0.2.0

- 增加严格首帧模式：默认禁止将原包装重构为纸盒、瓶子、罐子或其他包装形态。
- 商品事实现在会进入实际发送给视频模型的正向提示词，不只出现在脚本正文中。
- 修改目标、商品锁定、时长或画幅后自动废弃旧提示词，避免继续使用旧的 6 秒或旧包装脚本。
- 增加“复制原生 xAI 脚本”按钮，脚本会把 `images[0]` 传给 xAI 的 `image` 字段，并轮询 `/v1/videos/generations`。
- 增加模型配置说明：通用只发送 `prompt` 的视频脚本会忽略上游首帧图片。

## 0.1.0

- 首次可试版：商品首帧接入、动作与商品锁定字段、时间轴、镜头、声音、正向/动态负面提示词。
- 支持模板生成、可选 AI 润色、复制文本、创建正向提示词文本节点。
- 支持用当前画布视频模型和上游首帧参考图直接生成视频。
